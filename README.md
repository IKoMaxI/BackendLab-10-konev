# Lab10

ASP.NET Core Web API с логированием CRUD-операций через Serilog.

## Запуск

```bash
dotnet run
```

Swagger: `http://localhost:5106/swagger`.

Логи выводятся в консоль и сохраняются в каталоге `logs` с ежедневной ротацией.
